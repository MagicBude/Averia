# imports

这里保存 **Averia 统一导入格式** 的示例或人工准备文件。

- `examples/`：可以提交到 Git 的小型示例。
- 实际批次的暂存结果写入 `var/imports/`，默认不提交 Git。
- 外部来源原始大文件、Cookie、Token、私有凭据不要提交。

V0.2 先建立“统一导入合同”，后续 Provider 只需要把各来源数据转换成这个格式，再复用同一套匹配、审核和应用流程。
