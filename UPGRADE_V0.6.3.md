# Averia V0.6.3 升级说明

## 背景

首个真实 DMM Rental 在线抓取已经成功通过年龄确认并生成 canonical，但真实页面包含大量侧栏、导航和推荐链接。V0.6.2 的整页扁平文本解析会把这些区域里更早出现的「シリーズ」「ジャンル」误认为作品字段。

真实 `4ipzz698 / IPZZ-698` 回归中观察到：

- 正确系列应为 `引退作`，旧结果误为 `新人NO.1 STYLE`；
- 正确厂牌为 `ティッシュ`；
- Genres 应来自作品详情行，而旧结果混入 `アニメDVD`、其它女优姓名、`AV女優一覧へ` 等导航/推荐内容；
- 桃乃木かな、ZAMPA、アイデアポケット、140 分钟、封面、CID/番号、貸出開始日语义均已正确。

## 修复

V0.6.3 将 DMM 字段解析改为“有序作品详情字段簇”：

```text
貸出開始日
→ 収録時間
→ 出演者
→ 監督
→ シリーズ
→ メーカー
→ レーベル
→ ジャンル
→ 品番
```

只有定位到上述局部区域后才解析实体与 taxonomy。无法定位时直接失败，避免污染 canonical。

同时增加：

- 字段类型链接优先过滤；
- `一覧へ` 等导航标签过滤；
- 详情区品番与请求 CID 一致性检查；
- 真实页面噪声回归 Fixture。

## 升级后验证

```bash
pnpm check
```

然后无需再次联网，可直接使用上一轮成功保存的作品 `raw.html` 重新离线解析：

```bash
pnpm provider:dmm-rental -- \
  --file "<上一轮成功输出目录>/raw.html" \
  --url "https://www.dmm.co.jp/rental/ppr/-/detail/=/cid=4ipzz698/" \
  --code IPZZ-698
```

确认新 canonical 后再进入 Prepare；旧的污染 canonical 不得 Apply。
